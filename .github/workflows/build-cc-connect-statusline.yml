name: Build cc-connect statusline

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Clone upstream
        run: git clone --depth 1 https://github.com/chenhg5/cc-connect.git cc-connect

      - name: Patch statusline footer
        working-directory: cc-connect
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace(path, old, new):
              p = Path(path)
              s = p.read_text(encoding='utf-8')
              if old not in s:
                  raise SystemExit(f'missing pattern in {path}: {old[:80]!r}')
              p.write_text(s.replace(old, new, 1), encoding='utf-8')

          replace('config/config.go',
              'Display            DisplayConfig           `toml:"display"`\n\tStreamPreview      StreamPreviewConfig',
              'Display            DisplayConfig           `toml:"display"`\n\tStatuslineFooter   StatuslineFooterConfig  `toml:"statusline_footer"`\n\tStreamPreview      StreamPreviewConfig')

          replace('config/config.go',
              'type DisplayConfig struct {\n\tMode                 *string `toml:"mode"`                  // "full" (default), "compact", or "quiet"\n\tCardMode             *string `toml:"card_mode"`             // "legacy" (default) or "rich" (Card 2.0 Feishu)\n\tThinkingMessages     *bool   `toml:"thinking_messages"`     // whether thinking messages are shown; default true\n\tThinkingMaxLen       *int    `toml:"thinking_max_len"`      // max chars for thinking messages; 0 = no truncation; default 300\n\tToolMaxLen           *int    `toml:"tool_max_len"`          // max chars for tool use messages; 0 = no truncation; default 500\n\tToolMessages         *bool   `toml:"tool_messages"`         // whether tool progress messages are shown; default true\n\tShowContextIndicator *bool   `toml:"show_context_indicator"` // whether [ctx: ~N%] suffix is shown; default true\n\tReplyFooter          *bool   `toml:"reply_footer"`          // whether Codex-like footer is shown; default true\n}\n\n// StreamPreviewConfig',
              'type DisplayConfig struct {\n\tMode                 *string `toml:"mode"`                  // "full" (default), "compact", or "quiet"\n\tCardMode             *string `toml:"card_mode"`             // "legacy" (default) or "rich" (Card 2.0 Feishu)\n\tThinkingMessages     *bool   `toml:"thinking_messages"`     // whether thinking messages are shown; default true\n\tThinkingMaxLen       *int    `toml:"thinking_max_len"`      // max chars for thinking messages; 0 = no truncation; default 300\n\tToolMaxLen           *int    `toml:"tool_max_len"`          // max chars for tool use messages; 0 = no truncation; default 500\n\tToolMessages         *bool   `toml:"tool_messages"`         // whether tool progress messages are shown; default true\n\tShowContextIndicator *bool   `toml:"show_context_indicator"` // whether [ctx: ~N%] suffix is shown; default true\n\tReplyFooter          *bool   `toml:"reply_footer"`          // whether Codex-like footer is shown; default true\n}\n\n// StatuslineFooterConfig controls an optional Claude-style quota/model footer\n// appended to assistant replies before platform delivery.\ntype StatuslineFooterConfig struct {\n\tEnabled   *bool  `toml:"enabled,omitempty"`    // default false\n\tURL       string `toml:"url,omitempty"`        // quota endpoint\n\tToken     string `toml:"token,omitempty"`      // bearer/api key token\n\tTokenEnv  string `toml:"token_env,omitempty"`  // env var fallback for token\n\tTimeoutMs *int   `toml:"timeout_ms,omitempty"` // default 1500\n\tCacheSecs *int   `toml:"cache_secs,omitempty"` // default 30\n}\n\n// StreamPreviewConfig')

          replace('config/config.go',
              'ReplyFooter      *bool        `toml:"reply_footer,omitempty"`\n\tInjectSender     *bool',
              'ReplyFooter      *bool                   `toml:"reply_footer,omitempty"`\n\tStatuslineFooter *StatuslineFooterConfig `toml:"statusline_footer,omitempty"`\n\tInjectSender     *bool')

          replace('config/config.go',
              'return\n}\n\n// EffectiveCardMode',
              'return\n}\n\n// EffectiveStatuslineFooter resolves project-level statusline footer settings on\n// top of the global [statusline_footer] block.\nfunc EffectiveStatuslineFooter(cfg *Config, proj *ProjectConfig) StatuslineFooterConfig {\n\tif cfg == nil {\n\t\treturn StatuslineFooterConfig{}\n\t}\n\tout := cfg.StatuslineFooter\n\tif proj == nil || proj.StatuslineFooter == nil {\n\t\treturn out\n\t}\n\tp := proj.StatuslineFooter\n\tif p.Enabled != nil {\n\t\tout.Enabled = p.Enabled\n\t}\n\tif p.URL != "" {\n\t\tout.URL = p.URL\n\t}\n\tif p.Token != "" {\n\t\tout.Token = p.Token\n\t}\n\tif p.TokenEnv != "" {\n\t\tout.TokenEnv = p.TokenEnv\n\t}\n\tif p.TimeoutMs != nil {\n\t\tout.TimeoutMs = p.TimeoutMs\n\t}\n\tif p.CacheSecs != nil {\n\t\tout.CacheSecs = p.CacheSecs\n\t}\n\treturn out\n}\n\n// EffectiveCardMode')

          replace('core/engine.go', '"math"\n\t"os"', '"math"\n\t"net/http"\n\t"os"')
          replace('core/engine.go',
              'type replyFooterUsageCache struct {\n\ttext      string\n\tfetchedAt time.Time\n}\n\n// SaveRestartNotify',
              'type replyFooterUsageCache struct {\n\ttext      string\n\tfetchedAt time.Time\n}\n\n// StatuslineFooterCfg controls an optional Claude-style quota/model footer\n// fetched from an external quota endpoint before sending assistant replies.\ntype StatuslineFooterCfg struct {\n\tEnabled  bool\n\tURL      string\n\tToken    string\n\tTokenEnv string\n\tTimeout  time.Duration\n\tCacheTTL time.Duration\n}\n\ntype statuslineFooterCache struct {\n\ttext      string\n\tfetchedAt time.Time\n}\n\n// SaveRestartNotify')
          replace('core/engine.go',
              'replyFooterMu       sync.Mutex\n\treplyFooterUsage    replyFooterUsageCache\n\n\t// /web command callbacks',
              'replyFooterMu       sync.Mutex\n\treplyFooterUsage    replyFooterUsageCache\n\tstatuslineFooterCfg StatuslineFooterCfg\n\tstatuslineFooter    statuslineFooterCache\n\n\t// /web command callbacks')
          replace('core/engine.go',
              'func (e *Engine) SetReplyFooterEnabled(show bool) {\n\te.replyFooterEnabled = show\n}\n\n// SetFilterExternalSessions',
              'func (e *Engine) SetReplyFooterEnabled(show bool) {\n\te.replyFooterEnabled = show\n}\n\n// SetStatuslineFooterConfig configures an optional external quota/model footer.\nfunc (e *Engine) SetStatuslineFooterConfig(cfg StatuslineFooterCfg) {\n\tif cfg.Timeout <= 0 {\n\t\tcfg.Timeout = replyFooterUsageTimeout\n\t}\n\tif cfg.CacheTTL <= 0 {\n\t\tcfg.CacheTTL = replyFooterUsageCacheTTL\n\t}\n\te.replyFooterMu.Lock()\n\te.statuslineFooterCfg = cfg\n\te.statuslineFooter = statuslineFooterCache{}\n\te.replyFooterMu.Unlock()\n}\n\n// SetFilterExternalSessions')
          replace('core/engine.go',
              'if !e.replyFooterEnabled || agent == nil {\n\t\treturn ""\n\t}\n\n\tvar parts []string',
              'if !e.replyFooterEnabled || agent == nil {\n\t\treturn ""\n\t}\n\n\tmodel := replyFooterModel(session, agent)\n\tif footer := e.statuslineFooterText(model); footer != "" {\n\t\treturn footer\n\t}\n\n\tvar parts []string')
          replace('core/engine.go', 'if model := replyFooterModel(session, agent); model != "" {', 'if model != "" {')
          replace('core/engine.go',
              'func replyFooterModel(session AgentSession, agent Agent) string {',
              r'''func (e *Engine) statuslineFooterText(model string) string {
	e.replyFooterMu.Lock()
	cfg := e.statuslineFooterCfg
	cached := e.statuslineFooter
	e.replyFooterMu.Unlock()

	if !cfg.Enabled {
		return ""
	}
	if !cached.fetchedAt.IsZero() && time.Since(cached.fetchedAt) < cfg.CacheTTL {
		return cached.text
	}

	text := ""
	ctx, cancel := context.WithTimeout(e.ctx, cfg.Timeout)
	defer cancel()
	if quota, err := fetchStatuslineQuota(ctx, cfg); err == nil {
		text = formatStatuslineQuota(quota, model)
	} else {
		slog.Debug("statusline footer: fetch failed", "error", err)
		text = cached.text
	}

	e.replyFooterMu.Lock()
	e.statuslineFooter = statuslineFooterCache{text: text, fetchedAt: time.Now()}
	e.replyFooterMu.Unlock()
	return text
}

type statuslineQuotaPayload struct {
	Success bool `json:"success"`
	Data    struct {
		UsedUSD  float64 `json:"usedUsd"`
		LimitUSD float64 `json:"limitUsd"`
		Percent  int     `json:"percent"`
		ResetAt  string  `json:"resetAt"`
	} `json:"data"`
}

func fetchStatuslineQuota(ctx context.Context, cfg StatuslineFooterCfg) (*statuslineQuotaPayload, error) {
	url := strings.TrimSpace(cfg.URL)
	if url == "" {
		return nil, fmt.Errorf("statusline footer url is empty")
	}
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
	if err != nil {
		return nil, err
	}
	token := strings.TrimSpace(cfg.Token)
	if token == "" && cfg.TokenEnv != "" {
		token = strings.TrimSpace(os.Getenv(cfg.TokenEnv))
	}
	if token != "" {
		req.Header.Set("x-api-key", token)
		req.Header.Set("Authorization", "Bearer "+token)
	}

	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()
	if resp.StatusCode < 200 || resp.StatusCode >= 300 {
		body, _ := io.ReadAll(io.LimitReader(resp.Body, 512))
		return nil, fmt.Errorf("statusline footer endpoint returned %d: %s", resp.StatusCode, strings.TrimSpace(string(body)))
	}
	var payload statuslineQuotaPayload
	if err := json.NewDecoder(resp.Body).Decode(&payload); err != nil {
		return nil, err
	}
	if !payload.Success {
		return nil, fmt.Errorf("statusline footer endpoint success=false")
	}
	return &payload, nil
}

func formatStatuslineQuota(q *statuslineQuotaPayload, model string) string {
	if q == nil {
		return ""
	}
	percent := q.Data.Percent
	if percent < 0 {
		percent = 0
	}
	if percent > 100 {
		percent = 100
	}
	filled := percent / 10
	if filled < 0 {
		filled = 0
	}
	if filled > 10 {
		filled = 10
	}
	bar := strings.Repeat("\u2764\ufe0f", filled) + strings.Repeat("\U0001f90d", 10-filled)
	model = strings.TrimSpace(model)
	if model == "" {
		model = "unknown"
	}
	return fmt.Sprintf("%s%s  %s%.2f/%s%.0f  %s%s  %s%s%s",
		"\u2728",
		bar,
		"\U0001f4b2",
		q.Data.UsedUSD,
		"\U0001f4b2",
		math.Trunc(q.Data.LimitUSD),
		"\U0001f504",
		statuslineResetRemaining(q.Data.ResetAt),
		"\U0001f4bb",
		model,
		"\u2728",
	)
}

func statuslineResetRemaining(resetAt string) string {
	resetAt = strings.TrimSpace(resetAt)
	if resetAt == "" {
		return ""
	}
	resetTime, err := time.Parse(time.RFC3339, resetAt)
	if err != nil {
		return ""
	}
	remaining := time.Until(resetTime)
	if remaining <= 0 {
		return "0h"
	}
	if remaining >= 24*time.Hour {
		days := int(remaining.Hours()) / 24
		hours := int(remaining.Hours()) % 24
		return fmt.Sprintf("%dd%dh", days, hours)
	}
	if remaining >= time.Hour {
		hours := int(remaining.Hours())
		mins := int(remaining.Minutes()) % 60
		return fmt.Sprintf("%dh%dm", hours, mins)
	}
	return fmt.Sprintf("%dm", int(remaining.Minutes()))
}

func replyFooterModel(session AgentSession, agent Agent) string {''')

          replace('cmd/cc-connect/main.go',
              'engine.SetReplyFooterEnabled(showFooter)\n\t\tengine.SetAttachmentSendEnabled',
              'engine.SetReplyFooterEnabled(showFooter)\n\t\tengine.SetStatuslineFooterConfig(toCoreStatuslineFooterConfig(config.EffectiveStatuslineFooter(cfg, &proj)))\n\t\tengine.SetAttachmentSendEnabled')
          replace('cmd/cc-connect/main.go',
              'engine.SetReplyFooterEnabled(showFooter)\n\n\t// Reload auto-compress settings',
              'engine.SetReplyFooterEnabled(showFooter)\n\tengine.SetStatuslineFooterConfig(toCoreStatuslineFooterConfig(config.EffectiveStatuslineFooter(cfg, proj)))\n\n\t// Reload auto-compress settings')
          replace('cmd/cc-connect/main.go',
              'func derefInt(v *int) int {',
              '''func toCoreStatuslineFooterConfig(c config.StatuslineFooterConfig) core.StatuslineFooterCfg {
	enabled := c.Enabled != nil && *c.Enabled
	timeout := 1500 * time.Millisecond
	if c.TimeoutMs != nil && *c.TimeoutMs > 0 {
		timeout = time.Duration(*c.TimeoutMs) * time.Millisecond
	}
	cacheTTL := 30 * time.Second
	if c.CacheSecs != nil && *c.CacheSecs > 0 {
		cacheTTL = time.Duration(*c.CacheSecs) * time.Second
	}
	return core.StatuslineFooterCfg{
		Enabled:  enabled,
		URL:      c.URL,
		Token:    c.Token,
		TokenEnv: c.TokenEnv,
		Timeout:  timeout,
		CacheTTL: cacheTTL,
	}
}

func derefInt(v *int) int {''')
          PY

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: cc-connect/go.mod
          cache: true
          cache-dependency-path: cc-connect/go.sum

      - name: Format and test touched packages
        working-directory: cc-connect
        run: |
          gofmt -w cmd/cc-connect/main.go config/config.go core/engine.go
          go test ./config ./core -run 'TestProcessInteractiveEvents_AppendsReplyFooterWhenEnabled|TestProcessInteractiveEvents_AppendsContextIndicatorInsideReplyFooter'

      - name: Build Windows executable
        working-directory: cc-connect
        run: |
          mkdir -p dist
          BUILD_TIME="$(date -u '+%Y-%m-%dT%H:%M:%SZ')"
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -tags no_web -ldflags "-s -w -X main.version=v1.3.3-beta.4-statusline -X main.commit=statusline -X main.buildTime=${BUILD_TIME}" -o dist/cc-connect.exe ./cmd/cc-connect
          sha256sum dist/cc-connect.exe > dist/checksums.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cc-connect-statusline-windows-amd64
          path: |
            cc-connect/dist/cc-connect.exe
            cc-connect/dist/checksums.txt

# trigger: 2026-06-01T12:22:00Z
